/*

package federica

import co.theasi.plotly.{Plot,draw}

object PlotlyTest extends App {
  println{"Hello Federica"}
  val xs = (0.0 to 2.0 by 0.1)
  val ys = xs.map { x => x*x }

  val plot = Plot().withScatter(xs, ys)

  draw(plot, "my-first-plot")
}

 */
